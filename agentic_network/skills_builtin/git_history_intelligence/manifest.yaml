name: git_history_intelligence
version: 1.0
description: Bounded Git churn, co-change, ownership, and regression-hotspot evidence.
enabled: true
requires_user_approval: true
audit_enabled: true
permissions:
  network: DENY
  filesystem_read: ASK_ALWAYS
  filesystem_write: DENY
  git_read: ASK_ALWAYS
  git_write: DENY
  terminal_execute: ASK_ALWAYS
