name: internet_search
version: 1.0
description: Approved public web search through a fixed endpoint with bounded results and optional domain filtering.
enabled: true
requires_user_approval: true
audit_enabled: true
permissions:
  network: ASK_ALWAYS
  filesystem_read: DENY
  filesystem_write: DENY
  git_read: DENY
  git_write: DENY
  terminal_execute: DENY
