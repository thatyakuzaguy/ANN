name: observability_instrumentation
version: 1.0
description: Metrics, traces, logs, correlation IDs, dashboards, alerts, and instrumentation planning.
enabled: true
requires_user_approval: true
audit_enabled: true
permissions:
  network: DENY
  filesystem_read: ASK_ALWAYS
  filesystem_write: DENY
  git_read: DENY
  git_write: DENY
  terminal_execute: DENY
