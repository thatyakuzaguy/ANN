name: ml_evaluation
version: 1.0
description: Model-card, metric, reproducibility, drift, and bias evidence without training.
enabled: true
requires_user_approval: true
audit_enabled: true
permissions:
  network: DENY
  filesystem_read: ASK_ALWAYS
  filesystem_write: DENY
  git_read: DENY
  git_write: DENY
  terminal_execute: DENY
