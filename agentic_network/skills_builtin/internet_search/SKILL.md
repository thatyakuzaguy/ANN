# Internet Search

Searches the public web through ANN's fixed search endpoint after explicit
network permission and Approval Center review.

The search action accepts a bounded query, optional allowed_domains, and
max_results from 1 to 10. It returns titles, destination URLs, and snippets.
ANN does not open result pages, follow redirects, send credentials, or execute
content obtained from the web.
