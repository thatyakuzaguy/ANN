name: runtime_failure_lab
version: 1.0
description: Controlled interruption, resource, Docker, model-integrity, packaging, and recovery verification.
enabled: true
requires_user_approval: true
audit_enabled: true
permissions:
  network: DENY
  filesystem_read: ASK_ALWAYS
  filesystem_write: ASK_ALWAYS
  git_read: DENY
  git_write: DENY
  terminal_execute: ASK_ALWAYS
