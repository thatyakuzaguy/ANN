name: privacy_rights_verification
version: 1.0
description: Export, erasure, retention, consent, tenancy, audit, and approved privacy-rights execution evidence.
enabled: true
requires_user_approval: true
audit_enabled: true
permissions:
  network: DENY
  filesystem_read: ASK_ALWAYS
  filesystem_write: ASK_ALWAYS
  git_read: DENY
  git_write: DENY
  terminal_execute: ASK_ALWAYS
