name: cloud_deployment
version: 1.0
description: Provider-neutral identity, secret, region, cost, rollback, and deployment-planning evidence.
enabled: true
requires_user_approval: true
audit_enabled: true
permissions:
  network: DENY
  filesystem_read: ASK_ALWAYS
  filesystem_write: DENY
  git_read: DENY
  git_write: DENY
  terminal_execute: DENY
