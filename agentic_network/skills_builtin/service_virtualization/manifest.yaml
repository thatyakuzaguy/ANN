name: service_virtualization
version: 1.0
description: Deterministic workspace-only mock contracts for external service boundaries and failures.
enabled: true
requires_user_approval: true
audit_enabled: true
permissions:
  network: DENY
  filesystem_read: ASK_ALWAYS
  filesystem_write: DENY
  git_read: DENY
  git_write: DENY
  terminal_execute: DENY
