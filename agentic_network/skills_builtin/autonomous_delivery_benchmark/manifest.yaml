name: autonomous_delivery_benchmark
version: 1.0
description: End-to-end requirements, architecture, implementation, verification, release, rollback, and model-provenance benchmarks.
enabled: true
requires_user_approval: true
audit_enabled: true
permissions:
  network: DENY
  filesystem_read: ASK_ALWAYS
  filesystem_write: ASK_ALWAYS
  git_read: DENY
  git_write: DENY
  terminal_execute: ASK_ALWAYS
