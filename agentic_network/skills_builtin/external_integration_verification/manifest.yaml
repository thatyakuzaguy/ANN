name: external_integration_verification
version: 1.0
description: Provider, webhook, credential-boundary, idempotency, and approved HTTPS health checks.
enabled: true
requires_user_approval: true
audit_enabled: true
permissions:
  network: ASK_ALWAYS
  filesystem_read: ASK_ALWAYS
  filesystem_write: DENY
  git_read: DENY
  git_write: DENY
  terminal_execute: DENY
