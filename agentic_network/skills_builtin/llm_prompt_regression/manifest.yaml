name: llm_prompt_regression
version: 1.0
description: Golden prompt cases, output quality, format, latency, token, and regression comparison evidence.
enabled: true
requires_user_approval: true
audit_enabled: true
permissions:
  network: DENY
  filesystem_read: ASK_ALWAYS
  filesystem_write: DENY
  git_read: DENY
  git_write: DENY
  terminal_execute: DENY
