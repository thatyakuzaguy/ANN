name: agent_evaluation
version: 1.0
description: Golden-task scoring, outcome comparison, latency, retry, and agent-quality evidence.
enabled: true
requires_user_approval: true
audit_enabled: true
permissions:
  network: DENY
  filesystem_read: ASK_ALWAYS
  filesystem_write: DENY
  git_read: DENY
  git_write: DENY
  terminal_execute: DENY
